@using System.Security.Claims
@{
    Layout = "~/Views/Shared/Admin/_AdminLayout.cshtml";
}
<div class="main-content-container overflow-hidden">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4 mt-1">
        <h5 class="mb-0">Port Master</h5>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb align-items-center mb-0 lh-1">
                <li class="breadcrumb-item">
                    <a href="~/Admin/Dashboard" class="d-flex align-items-center text-decoration-none">
                        <i class="ri-home-8-line fs-15 text-primary me-1"></i>
                        <span class="text-body fs-14 hover">Dashboard</span>
                    </a>
                </li>
                <li class="breadcrumb-item active" aria-current="page">
                    <span>Port Master</span>
                </li>
            </ol>
        </nav>
    </div>
    <div class="row">
        <div class="col-xxl-12 col-lg-12 col-md-12 col-12">
            <div class="table-card">
                <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 p-3">
                    <form class="table-src-form position-relative m-0">
                        <input type="text" id="txtSearch" class="form-control" placeholder="Search here...">
                        <div class="src-btn">
                            <span class="ti ti-search">search</span>
                        </div>
                    </form>
                    <button type="button" class="btn btn-md btn-primary" data-bs-toggle="modal" data-bs-target="#addform"><i class="ti ti-plus"></i> Add Port</button>
                </div>
                <div class="table-responsive">
                    <table id="portTable" class="table align-middle w-100">
                        <thead>
                            <tr>
                                <th scope="col" class="fw-medium">Sno</th>
                                <th scope="col" class="fw-medium">Port Name</th>
                                <th scope="col" class="fw-medium"><abbr data-title="Date of Entry">DOE</abbr></th>
                                <th scope="col" class="fw-medium">Created by</th>
                                <th scope="col" class="fw-medium">Status</th>
                                <th scope="col" class="fw-medium">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                        </tbody>
                    </table>
                    <div class="d-flex justify-content-center justify-content-sm-between align-items-center text-center flex-wrap gap-2 showing-wrap px-3 pb-3">
                        <span class="fs-15" id="lblpagedescription">Showing 1 to 10 of 50 entries</span>

                        <nav class="custom-pagination" aria-label="Page navigation">
                            <ul class="pagination mb-0 justify-content-center" id="pagination">
                            </ul>
                        </nav>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal Add New Task -->
<div class="modal fade" id="addform" tabindex="-1" aria-labelledby="portModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-slideout" role="document">
        <div class="modal-content bg-white">
            <div class="modal-header">
                <h5 class="modal-title mb-0" id="portModalLabel">Add Port</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                    <i class="ti ti-x"></i>
                </button>
            </div>
            <div class="modal-body p-3">
                <input type="hidden" id="hdnPortId" />
                <div class="row">
                    <div class="col-lg-12">
                        <div class="mb-3">
                            <label class="label">Port Name</label>
                            <input type="text" class="form-control" id="txtPortName" placeholder="Port Name">
                        </div>
                        <div class="mb-3">
                            <label class="label">Status</label>
                            <select class="form-select form-control" id="ddlPortStatus" aria-label="Floating label select example">
                                <option value="-1" selected>Select Status</option>
                                <option value="1">Active</option>
                                <option value="0">In-Active</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer p-3">
                <button type="button" class="btn btn-md btn-danger fw-normal text-white" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-md btn-primary fw-normal text-white" id="btnAdd">Submit</button>
            </div>
        </div>
    </div>
</div>
<!-- Modal confirmation-->
@section Scripts {
    <script>
         let portData = [];
        let pageSize = 10;         // records per page
        let currentPage = 1;
        let filteredData = [];
        // $(document).ready(function () {
        //     LoadTerminalList();
        // });
        function validateContols() {
            if (!validateRequired("txtPortName", ["", " "], "Port name is required !")) return false;
            if (!validateRequired("ddlPortStatus", ["-1", ""], "Please select status !")) return false;
            return true;
        }
        $(document).on("click", "#btnAdd", function () {
            if (!validateContols()) {
                return;
            }
            let PortName = $("#txtPortName").val().trim();
            let Status = $("#ddlPortStatus").val();
            let payload = {
                portName: PortName,
                isActive: Status === "1" ? true : false,
                createdBy: 0
            };

            $.ajax({
                url: "/Master/AddPort",
                type: "POST",
                contentType: "application/json",
                data: JSON.stringify(payload),
                success: function (resp) {
                    let type = resp.status === 1 ? "success" : resp.status === 2 ? "warning" : "error";
                    let title = resp.status === 1 ? "Success" : resp.status === 2 ? "Already Exists" : "Error";

                    if (resp.status === 1) {
                        ResetPortForm();
                        // LoadTerminalList();
                    }
                    ShowToast(type, title, resp.message);

                }
            });
        });
        $(document).on("input change", "#txtPortName, #ddlPortStatus", function () {
            $(this).removeClass("input-error");
            $(this).next(".error-text").remove();
        });
        $(document).on("click", '[data-bs-target="#addform"]', function () {
            ResetPortForm();
        });
        function ResetPortForm() {

            // Close modal
            var modalEl = $("#addform");
            var modal = bootstrap.Modal.getInstance(modalEl);
            if (modal) modal.hide();

            // Clear controls
            $("#txtPortName").val("");
            $("#ddlPortStatus").val("-1");

            // Clear validation errors
            $("#txtPortName, #ddlPortStatus").removeClass("input-error");
            $(".error-text").remove();

            //Refresh the button and modal
            $("#btnUpdate").text("Submit").attr("id", "btnAdd");
            $("#portModalLabel").text("Add Port");
        }
</script>
}